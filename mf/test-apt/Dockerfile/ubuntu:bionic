FROM ubuntu:bionic

RUN apt-get update -y && \
    apt-get install -y wget unzip python-stdeb  dh-python git

COPY docker-entrypoint.sh /docker-entrypoint.sh

ENTRYPOINT ["/bin/bash", "/docker-entrypoint.sh"]
